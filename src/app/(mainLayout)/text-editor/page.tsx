import TextEditor from '@/components/pages/TextEditor/TextEditor';
import React from 'react';

const page = () => {
    return (
        <div>
            <TextEditor />
        </div>
    );
};

export default page;